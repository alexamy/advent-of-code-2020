use crate::reader;

pub fn solve() -> u32 {
    let input = reader::read_input();

    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn is_solved() {
        assert_eq!(solve(), 0);
    }
}
